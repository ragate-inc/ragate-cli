export type Locale = {
  error: {
    alreadyExistsDirectory: string;
    unProcessed: string;
  };
  inquirer: {
    template: {
      choiceTemplate: string;
      autocomplete: {
        emptyText: string;
      };
    };
  };
};
