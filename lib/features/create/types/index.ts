export type Locale = {
  error: {
    alreadyExistsDirectory: string;
  };
  inquirer: {
    template: {
      choiceTemplate: string;
      autocomplete: {
        emptyText: string;
      };
    };
  };
};
